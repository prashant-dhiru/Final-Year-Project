import { GraphData, ColorScheme } from './graph-data';

export class GraphDataSet {
    constructor (
        graphData: GraphData[],
        graphColorScheme: ColorScheme,
        graphSize?: number[]
    ) {}
}
