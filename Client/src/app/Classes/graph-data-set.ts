import { GraphData, ColorScheme } from './index';

export class GraphDataSet {
    constructor (
        graphData: GraphData[],
        graphColorScheme: ColorScheme,
        graphSize: number[]
    ) {}
}
